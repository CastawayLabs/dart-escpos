name: escpos
description: Dart library for ESC/POS with networking capability
version: 6.0.1-test
homepage: https://github.com/CastawayLabs/dart-escpos
environment:
  sdk: ">=3.0.0-0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  gbk_codec_nohtml: 1.0.0
  image:
  hex:
dev_dependencies:
  flutter_lints:
  flutter_test:
    sdk: flutter
flutter:
  assets:
    - packages/escpos/resources/capabilities.json
